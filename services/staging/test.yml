_format_version: "3.0"
_info:
  defaults: {}
  select_tags:
    - test

services:
  - name: test
    enabled: true
    host: test
    port: 443
    tls_verify: false
    protocol: https
    routes:
      - name: test-route
        hosts:
          - test.com
        methods:
          - POST
          - GET
          - PATCH
        protocols:
          - http
        paths:
          - /auth/teste
        strip_path: false

    tags:
      - test