_format_version: "3.0"
_info:
  defaults: {}
  select_tags:
    - testing

services:
  - name: testing
    enabled: true
    host: test
    port: 443
    tls_verify: false
    protocol: https
    routes:
      - hosts:
          - test.com
        name: test-route2
        methods:
          - GET
        paths:
          - /auth/teste2
        protocols:
          - https
        strip_path: false

    tags:
      - testing

# deixando as keys principais corretas, mas errado dentro de routes para teste

