# Arquivo: .github/workflows/sync-regions.yml

name: (Reusable Test) Sync Region
on:
  workflow_call:
    inputs:
      region_name:
        type: string
        required: true
      environment_name:
        type: string
        required: true

jobs:
  apply:
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment_name }}
    steps:
      - name: Simulate Apply
        run: |
          echo "✅ Simulando apply no cluster: ${{ inputs.region_name }}"
          sleep 5